# Docker Compose file Reference (https://docs.docker.com/compose/compose-file/)

version: '3'

services:

  app_flask:
    build:
      context: . 
      dockerfile: Dockerfile.local
    ports:
      - "8081:8000" 
    restart: unless-stopped
    volumes:
    - '~/.kube/config:/app_flask/kube/config'
    # - '~/.kube/:/root/.kube/'
    # environment: 
    #   KUBECONFIG: /app_flask/kube/config
